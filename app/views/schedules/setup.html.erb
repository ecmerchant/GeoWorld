<div class="container">
  <div class="page-header" id="banner">
    <div class="row my-4">
      <div class="col-lg-10 col-md-10">
        <h2>スケジュールの設定</h2>
        <%= form_tag('/schedules/setup', method:'post') do %>
          <div class="actions">
            <%= submit_tag '設定する', class: 'btn btn-info', style: 'margin-right:20px;' %>
          </div><br>
          <table class="table table-striped table-hover table-bordered" style='width:600px; table-layout:fixed;'>
            <thead class="thead-light">
              <tr>
                <th style='width:100px;'>番号</th>
                <th style='width:150px;'>ファイルタイプ</th>
                <th style='width:120px;'>実行タイミング</th>
                <th style='width:230px;'>実行時間</th>
              </tr>
            </thead>
            <tbody>
              <% for int in 1..10 do %>
              <tr>
                <td class="td_vertical_align_middle"><%= int %><%= hidden_field_tag 'shcedule_num[' + int.to_s + ']', int %></td>
                <td class="td_vertical_align_middle"><%= select_tag 'list_type[' + int.to_s + ']',  options_for_select(["","新規出品", "出品削除"], @schedules.find_or_create_by(schedule_num: int).list_type), class:'form-control' %></td>
                <td class="td_vertical_align_middle"><%= select_tag 'schedule_type[' + int.to_s + ']',  options_for_select(["毎日", "平日", "月曜日", "火曜日", "水曜日", "木曜日", "金曜日", "土曜日", "日曜日"], @schedules.find_or_create_by(schedule_num: int).schedule_type), class:'form-control' %></td>
                <td class="td_vertical_align_middle form-inline"><%= time_select 'set_time[' + int.to_s + ']',   @schedules.find_or_create_by(schedule_num: int).set_time, {minute_step: 5} , class:'form-control', style:'width:70px;' %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        <% end %>
      </div>
    </div>
  </div>
</div>
